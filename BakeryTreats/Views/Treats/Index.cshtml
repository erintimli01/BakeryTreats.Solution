@{
  Layout = "_Layout";
}

@using BakeryTreats.Models;

<h1>Treats</h1>

@if (@Model.Count == 0)
{
  <h3>There are no treats added yet!</h3>
} 
else
{
  <ul>
    @foreach (Treat treat in Model)
    {
      <li>@Html.ActionLink($"{treat.Name}", "Details", new { id = treat.TreatId })</li>
    }
  </ul>
}

<p>@Html.ActionLink("Add new treat", "Create")</p>
